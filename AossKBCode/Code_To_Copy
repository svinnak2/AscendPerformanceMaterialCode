import re

# Example text with control characters
text = "Hello,\nWorld!\tThis is a test.\x0c"

# Remove control characters using regex
cleaned_text = re.sub(r'[\x00-\x1F\x7F]', '', text)
print(cleaned_text)  # Output: "Hello,World!This is a test."
